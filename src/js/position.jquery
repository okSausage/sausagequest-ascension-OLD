$(function () {
  $(".hv_center").css({
    position: "absolute",
    left: "50%",
    top: "50%",
    "margin-left": -$(".hv_center").outerWidth() / 2,
    "margin-top": -$(".hv_center").outerHeight() / 2,
  });
});
